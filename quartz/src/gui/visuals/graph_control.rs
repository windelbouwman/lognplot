/*
// TODO: move below to widget library?

/// A graph visualization control!
struct GraphControl {
    // traces: Vec<TraceVisual>,
}

impl GraphControl {
    fn draw(&self) {
        for trace in self.traces.iter() {
            // Draw!
        }
    }
}
*/
